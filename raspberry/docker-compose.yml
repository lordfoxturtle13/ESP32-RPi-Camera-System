services:
  camera-server:
    build: .
    restart: always
    ports:
      - "5000:5000"
    volumes:
      - ./config.json:/app/config.json
      - ./archivum:/app/archivum
      - /etc/tailscale-certs:/etc/tailscale-certs:ro
    devices:
      - /dev/video10:/dev/video10
      - /dev/video11:/dev/video11
      - /dev/video12:/dev/video12
